const {
  addSchema,
  updateSchema,
  updateFavouriteSchema,
} = require("./contacts");
const contactsDbSchema = require("./contactsDbSchema");

module.exports = {
  addSchema,
  updateSchema,
  updateFavouriteSchema,
  contactsDbSchema,
};
